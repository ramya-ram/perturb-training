package code;

/**
 * An enum specifying the current experimental condition:
 * 
 * -perturbation training using HR-Perturb
 * -procedural training using Q-learning
 * -perturbation training using Q-learning
 */
public enum ExperimentCondition {
	HRPR, PROCE_Q, PERTURB_Q
}
